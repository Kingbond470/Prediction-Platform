# Environment variables (never commit credentials)
.env

# Python virtual environment
venv/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/

# Jupyter
.ipynb_checkpoints/

# Large raw data (re-downloadable from Kaggle)
data/raw/

# Large intermediate processed files (regenerated by pipeline)
data/processed/cleaned_matches.csv
data/processed/cleaned_deliveries.csv
data/processed/feature_matrix.csv

# External fixtures (committed separately)
# data/external/  ← keep tracked

# Note: predictions_2026.csv, training_results.json, best_model.pkl, scaler.pkl
# are intentionally tracked — they are the pipeline outputs used by the website.

# Logs
logs/

# OS
.DS_Store
